import React from 'react';
import ReactDOM from 'react-dom/client';
import StudyPlan from './study_command_center.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <StudyPlan />
  </React.StrictMode>
);
